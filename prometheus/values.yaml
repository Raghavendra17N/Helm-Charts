# Default values for prometheus.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.


deployment:
  name: prometheus
  namespace: 
    create: true #falase leads to use default namespace
    name: monitoring
rolename: prometheus-role



